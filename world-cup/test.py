# Simulate a sports tournament

import csv
import sys
import random

teams = []
counts = {}

counts["apple"] = 1
counts["apple"] = counts["apple"] +1

print(counts)

i = 0

while i != 5:
    i = i+1
    print(i)

"""
if len(sys.argv) != 2:
    sys.exit("Usage: python tournament.py FILENAME")
"""
"""
with open(sys.argv[1]) as file:
    file_reader = csv.DictReader(file)
    # add each teams in the  list
    for team in file_reader:
        teams.append(team)
"""

