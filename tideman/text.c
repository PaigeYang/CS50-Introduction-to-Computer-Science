#include <cs50.h>
#include <stdio.h>
#include <string.h>
#include <ctype.h>


typedef struct
{
    int winner;
    int loser;
}
pair;

pair pairs[0];


int main(void)
{
 
pairs[0].winner = 2;
pairs[0].loser = 30;
 
printf("%i", pairs[0]);
   
}